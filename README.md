# Infinite runner

This repo is a simple endless HTML5 runner game written with Phaser. It uses Vite for bundling.

#### How to install:

```bash
cd project-name
npm install
```

#### How to run:

```bash
npm run dev
```
